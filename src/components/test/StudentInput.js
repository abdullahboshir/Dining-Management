import React from 'react';

const StudentInput = (props) => {
    // console.log(props?.text)
    return (
        <div>
            {/* <p className='mb-1'>Student ID</p>
            <input name={props.studentId} type={props.text} placeholder="Type here" className="input input-bordered input-success w-full h-10 max-w-xs mb-4" /> */}
        </div>
    );
};

export default StudentInput;